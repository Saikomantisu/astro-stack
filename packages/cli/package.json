{
  "name": "@astro-stack/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "astro-stack": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@astro-stack/generator": "workspace:*",
    "@astro-stack/utils": "workspace:*"
  }
}
