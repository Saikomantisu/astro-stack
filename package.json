{
  "name": "astro-stack",
  "version": "0.1.0",
  "private": true,
  "description": "Production-ready Astro project generator.",
  "packageManager": "pnpm@11.0.8",
  "scripts": {
    "build": "pnpm --recursive run build",
    "check": "pnpm --recursive run check",
    "clean": "pnpm --recursive run clean"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
